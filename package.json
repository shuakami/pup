{
  "name": "neural-link",
  "version": "1.0.0",
  "private": true,
  "description": "NEURAL-LINK: persistent, fault-tolerant browser interface for text-only agents (JSON-lines RPC).",
  "main": "src/index.js",
  "bin": {
    "neural-link": "src/index.js"
  },
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.7.8",
    "puppeteer-core": "^24.2.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
